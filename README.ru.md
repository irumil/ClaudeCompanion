# ClaudeCompanion

Приложение в системном трее для мониторинга квоты Claude.ai API в реальном времени.

![Architecture](schema/architecture-simple.txt)

## Возможности

- 🎯 **Мониторинг в реальном времени** - Обновление каждые 30 секунд
- 🎨 **Динамическая иконка в трее** - Показывает остаток квоты в процентах с цветовой кодировкой
- 🔔 **Умные уведомления** - Предупреждает, когда квота заканчивается
- ☀️ **Утренний привет Клоду** - Автоматическая отправка сообщения по расписанию для оптимизации 5-ти часового лимита
- 🌐 **Интеграция с браузером** - Расширение для Firefox для бесшовной аутентификации
- ⚙️ **Горячая перезагрузка настроек** - Не требует перезапуска при изменении конфигурации
- 🔒 **Поддержка прокси** - Работает с корпоративными прокси-серверами
- 📊 **Подробные подсказки** - Показывает информацию о квоте за 5 часов и 7 дней

## Цветовая индикация

- 🟢 **Зелёный** (> 40%) - Много квоты
- 🟡 **Жёлтый** (20-40%) - Умеренное использование
- 🔴 **Красный** (< 20%) - Низкая квота
- ⚪ **Серый** - Ошибка подключения

## ⚠️ Предупреждение и риски

### Важная информация перед использованием

**ClaudeCompanion использует неофициальный API Claude.ai.** Это означает:

1. **Потенциальный риск блокировки аккаунта**
   - Приложение обращается к внутреннему API, не предназначенному для публичного использования
   - Claude.ai может расценить это как нарушение условий использования
   - Хотя риск низкий при разумном использовании, мы не можем его полностью исключить

2. **Автоматизация может нарушать ToS**
   - Автоматические запросы каждые 30-120 секунд
   - Автоматическая отправка сообщений (функция greeting)
   - Может быть расценено как ботовая активность

### ✅ Рекомендации для безопасного использования

1. **Увеличьте интервал опроса**
   ```yaml
   poll_interval_seconds: 60  # Минимум 60 секунд
   # или для большей безопасности:
   poll_interval_seconds: 120  # 2 минуты
   ```

2. **Аккуратно используйте функцию greeting**
   ```yaml
   greeting:
     greeting_cron: "0 8 * * *"  # ✅ Раз в день - безопасно
     # greeting_cron: "*/5 * * * *"  # ❌ Каждые 5 минут - ОПАСНО
   ```

3. **Используйте только для личных целей**
   - Один аккаунт
   - Не используйте для массовой автоматизации
   - Не запускайте на нескольких аккаунтах одновременно

4. **Не держите запущенным 24/7**
   - Запускайте только когда работаете
   - Останавливайте на ночь и в выходные

### 📊 Оценка рисков

**Риск НИЗКИЙ**, если:
- ✅ Интервал опроса ≥ 60 секунд
- ✅ Greeting максимум 1-2 раза в день
- ✅ Личное использование на одном аккаунте
- ✅ Запуск только в рабочее время

**Риск ВЫСОКИЙ**, если:
- ❌ Частые запросы каждые 5-10 секунд
- ❌ Частые автоматические сообщения
- ❌ Использование на множестве аккаунтов
- ❌ Работа 24/7 без перерывов

### 🎯 Дисклеймер

**Используйте на свой страх и риск.** Разработчики не несут ответственности за возможную блокировку аккаунта или другие последствия использования приложения. Официальный и безопасный способ работы с Claude.ai - использование только веб-интерфейса на сайте claude.ai.

Если Claude.ai важен для вашей работы, рассмотрите возможность использования только официального веб-интерфейса.

## Установка

### 1. Скачивание

Скачайте последний релиз из [Releases](../../releases) или соберите из исходников.

### 2. Настройка

Скопируйте `config.yaml.example` в `config.yaml` и настройте параметры:

```yaml
proxy: "http://ваш-прокси:порт"  # Опционально: ваш прокси-сервер
browser_path: "C:\\Program Files\\Mozilla Firefox\\firefox.exe"  # Опционально: путь к браузеру
enable_file_logging: false  # true = логирование в файл, false = только консоль
```

### 3. Установка расширения для браузера

1. Откройте Firefox
2. Перейдите на `about:debugging#/runtime/this-firefox`
3. Нажмите "Загрузить временное дополнение"
4. Выберите файл `extension/manifest.json`

### 4. Запуск

Дважды кликните на `claudecompanion.exe` - приложение запустится в системном трее (без консольного окна).

## Использование

1. **Запустите приложение** - Запустите `claudecompanion.exe`
2. **Откройте Claude.ai** в Firefox - Расширение автоматически отправит данные аутентификации
3. **Проверьте иконку в трее** - Показывает остаток квоты в процентах
4. **Клик правой кнопкой мыши** по иконке для меню:
   - "Открыть Claude.ai" - Открыть Claude.ai в браузере
   - "Открыть настройки" - Редактировать конфигурацию
   - "Выход" - Закрыть приложение

## Конфигурация

Все настройки находятся в файле `config.yaml`:

### Основные настройки

```yaml
server_port: 8383              # Порт для подключения расширения браузера
poll_interval_seconds: 30      # Как часто проверять квоту
proxy: ""                      # HTTP прокси (оставьте пустым, если не нужен)
browser_path: ""               # Путь к браузеру (оставьте пустым для браузера по умолчанию)
enable_file_logging: false     # Включить логирование в файл
```

### Настройки уведомлений

```yaml
low_value_notifications:
  enabled: true
  threshold: 20                # Уведомлять, когда квота <= 20%
  phrases:                     # Случайные фразы для низкой квоты
    - "Пора идти домой! 🏡"
    - "Время отдохнуть! ☕"
  zero_phrases:                # Случайные фразы для нулевой квоты
    - "Всё, капут! 💥"
    - "Game over! 🎮"
```

### Демо-режим

Для тестирования всех функций и уведомлений:

```yaml
demo_mode:
  enabled: true
  duration_seconds: 60         # Симулирует изменение от 100% → 0% за 60 секунд
```

В demo режиме демонстрируются:
- Изменение иконки от 100% до 0% (зелёный → жёлтый → красный)
- Уведомление "Утренний привет Клоду" в начале каждого цикла ☀️
- Уведомления о низкой квоте (при достижении порога)
- Уведомления о нулевой квоте

### Утренний привет Клоду

Автоматическая отправка сообщения в чат по расписанию для оптимизации 5-ти часового лимита:

```yaml
greeting:
  greeting_cron: "0 8 * * *"   # Cron расписание: 8 утра каждый день
  greeting_text: "Ok"          # Текст сообщения
  greeting_chat_id: ""         # UUID чата (обязательно для работы)
```

**Как получить UUID чата:**
1. Откройте нужный чат на claude.ai
2. UUID находится в URL: `https://claude.ai/chat/{UUID}`
3. Скопируйте UUID в настройку `greeting_chat_id`

**Примеры cron расписаний:**
- `"0 8 * * *"` - каждый день в 8:00
- `"0 9 * * 1-5"` - в 9:00 по будням (пн-пт)
- `"30 7 * * *"` - каждый день в 7:30
- `"*/5 * * * *"` - каждые 5 минут (для тестирования)

При отправке приветствия появится уведомление "Утренний привет Клоду" ☀️

### Рабочие часы

Ограничение опроса API определёнными временными интервалами (например, только в рабочее время):

```yaml
work_hours:
  enabled: false              # Включить ограничение опроса только рабочими часами
  start: "08:00"              # Время начала (формат HH:MM)
  end: "20:00"                # Время окончания (формат HH:MM)
```

**Как это работает:**
- При `enabled: true` опрос API происходит только между временем `start` и `end`
- Использует 24-часовой формат (HH:MM)
- Поддерживает ночные интервалы (например, `start: "20:00"`, `end: "08:00"` для ночной смены)
- Вне рабочих часов опрос пропускается (API запросы не выполняются)
- Помогает снизить ненужное потребление API и риск обнаружения

**Примеры:**
- `start: "08:00"`, `end: "20:00"` - типичный рабочий день (с 8 утра до 8 вечера)
- `start: "09:00"`, `end: "17:00"` - стандартные офисные часы (с 9 до 5)
- `start: "20:00"`, `end: "08:00"` - ночная смена

## Архитектура

Приложение состоит из двух частей:

### 1. Расширение для браузера (Firefox)
- Извлекает cookie `sessionKey` с Claude.ai
- Получает UUID организации
- Отправляет данные аутентификации и organizationId в десктопное приложение

### 2. Десктопное приложение (Go)
- **HTTP Server** - Получает данные от расширения браузера
- **API Client** - Опрашивает Claude.ai API каждые 30 секунд
- **Cron Scheduler** - Отправляет приветствия по расписанию
- **Tray Manager** - Показывает динамическую иконку с процентами
- **Icon Generator** - Создаёт иконки 48x48 с цветными цифрами
- **Notifier** - Windows Toast уведомления
- **Config Manager** - Горячая перезагрузка конфигурации
- **Logger** - Опциональное логирование в файл

Смотрите [schema/architecture.md](schema/architecture.md) для подробной схемы архитектуры.

## Сборка из исходников

### Требования

- Go 1.21 или выше
- Windows (для системного трея и уведомлений)
- Git (опционально, для клонирования репозитория)

### Полная сборка приложения и расширения

#### Вариант 1: Автоматическая сборка (рекомендуется)

Самый простой способ - использовать готовый скрипт:

```bash
# Собрать всё (приложение + расширение)
cd build
build-all.bat

# Или только отладочную версию с консолью
build-debug.bat
```

#### Вариант 2: Быстрая сборка вручную

```bash
# 1. Клонировать репозиторий (или скачать ZIP)
git clone https://github.com/your-username/ClaudeCompanion.git
cd ClaudeCompanion

# 2. Установить зависимости
go mod download

# 3. Собрать приложение (релизная версия без консоли)
go build -ldflags "-H windowsgui" -o dist/claudecompanion.exe ./cmd/claudecompanion

# 4. Скопировать необходимые файлы
copy icon.ico dist\
copy config.yaml.example dist\config.yaml

# 5. Собрать расширение для браузера
cd build
package-extension.bat
cd ..
```

Готово! Приложение находится в `dist/claudecompanion.exe`, расширение в `dist/claudecompanion-extension.zip`

#### Вариант 3: Сборка с иконкой в exe

Для встраивания иконки в exe файл (для более красивых уведомлений):

```bash
# 1-2. Те же шаги, что выше

# 3. Установить компилятор ресурсов (один раз)
go install github.com/akavel/rsrc@latest

# 4. Сгенерировать ресурсы Windows
cd cmd/claudecompanion
rsrc -ico "..\..\icon.ico" -o rsrc_windows_amd64.syso
cd ..\..

# 5. Собрать с встроенной иконкой
go build -ldflags "-H windowsgui" -o dist/claudecompanion.exe ./cmd/claudecompanion

# 6. Скопировать файлы
copy icon.ico dist\
copy config.yaml.example dist\config.yaml

# 7. Собрать расширение
cd build
package-extension.bat
```

### Отладочная сборка (с консолью)

Для разработки и отладки (с видимой консолью):

```bash
# Сборка с консолью для просмотра логов
go build -o dist/claudecompanion-debug.exe ./cmd/claudecompanion

# Запуск в консоли
dist\claudecompanion-debug.exe
```

### Только расширение

Если нужно пересобрать только браузерное расширение:

```bash
cd build
package-extension.bat
```

Или вручную:

```bash
cd extension
powershell -Command "Compress-Archive -Path manifest.json,background.js,options.html,options.js,icon48.png,icon96.png -DestinationPath ../dist/claudecompanion-extension.zip -Force"
```

### Проверка сборки

После сборки проверьте:

```bash
# Проверить наличие файлов
dir dist

# Должны быть:
# claudecompanion.exe (релиз) или claudecompanion-debug.exe (отладка)
# claudecompanion-extension.zip
# config.yaml
# icon.ico
```

## Структура проекта

```
ClaudeCompanion/
├── cmd/
│   └── claudecompanion/
│       └── main.go              # Точка входа приложения
├── internal/
│   ├── api/                     # Клиент Claude.ai API
│   ├── config/                  # Управление конфигурацией
│   ├── icon/                    # Генератор динамических иконок
│   ├── logger/                  # Система логирования
│   ├── notifier/                # Toast уведомления
│   ├── server/                  # HTTP сервер для расширения
│   └── tray/                    # Менеджер системного трея
├── extension/
│   ├── manifest.json            # Манифест расширения Firefox
│   ├── background.js            # Логика расширения
│   └── icon.png                 # Иконка расширения
├── dist/
│   ├── claudecompanion.exe      # Собранное приложение
│   ├── config.yaml              # Конфигурация пользователя (не в git)
│   └── icon.ico                 # Иконка для уведомлений
├── schema/
│   ├── architecture.md          # Подробная архитектура
│   └── architecture-simple.txt  # ASCII диаграмма
├── go.mod
├── go.sum
├── icon.ico                     # Иконка приложения
├── config.yaml.example          # Пример конфигурации
├── README.md                    # English documentation
└── README.ru.md                 # Русская документация
```

## Решение проблем

### Расширение не работает
- Проверьте, что Firefox запущен
- Перезагрузите расширение в `about:debugging`
- Откройте консоль браузера для просмотра ошибок

### Иконка не обновляется
- Проверьте, что вы посетили Claude.ai в Firefox
- Убедитесь, что расширение отправило данные (проверьте логи, если включены)
- Попробуйте перезапустить приложение

### Ошибки прокси
- Проверьте адрес прокси в config.yaml
- Протестируйте прокси вручную с помощью curl
- Проверьте аутентификацию на прокси, если требуется

### Уведомления не показываются
- Включите уведомления в настройках Windows
- Проверьте пороги уведомлений в настройках
- Попробуйте демо-режим для тестирования уведомлений

## Зависимости

### Библиотеки Go
- [github.com/getlantern/systray](https://github.com/getlantern/systray) - Системный трей
- [github.com/go-toast/toast](https://github.com/go-toast/toast) - Toast уведомления
- [github.com/robfig/cron/v3](https://github.com/robfig/cron) - Планировщик задач по расписанию
- [gopkg.in/yaml.v3](https://gopkg.in/yaml.v3) - Парсинг YAML

### Внешние инструменты
- **curl.exe** - Для API запросов (входит в Windows 10+)
- **notepad.exe** - Для открытия файла конфигурации

## Лицензия

[Ваша лицензия здесь]

## Участие в разработке

Приветствуются любые вклады! Пожалуйста, создавайте Pull Request'ы.

## Поддержка

По вопросам и проблемам создавайте issue на GitHub.

---

🇬🇧 [English version](README.md) | 🇷🇺 Русская версия | [Татарча](README.tt.md)
